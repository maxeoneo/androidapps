image: jangrewe/gitlab-ci-android

before_script:
  - mkdir outputs
#   - export GRADLE_USER_HOME=$(pwd)/RollingDice/.gradle
#   - chmod +x ./RollingDice/gradlew

# cache:
#   key: ${CI_PROJECT_ID}
#   paths:
#     - .gradle/

stages:
  - build
  - test
  - release
  - deploy

/* Calculator *****************************************************************/
lintDebugCalculator:
  stage: build
  script:
    - ./Calculator/gradlew -Pci --console=plain :app:lintDebug -PbuildDir=lint

assembleDebugCalculator:
  stage: build
  script:
    - ./Calculator/gradlew assembleDebug

testDebugCalculator:
  stage: test
  script:
    - ./Calculator/gradlew -Pci --console=plain :app:testDebug

assembleReleaseCalculator:
  stage: release
  script:
    - echo $KEYSTORE_FILE | base64 -d > my.keystore
    - ./Calculator/gradlew assembleRelease
      -Pandroid.injected.signing.store.file=$(pwd)/my.keystore
      -Pandroid.injected.signing.store.password=$KEYSTORE_PASSWORD
      -Pandroid.injected.signing.key.alias=$KEY_ALIAS
      # -Pandroid.injected.signing.key.password=$KEY_PASSWORD
    - mv ./Calculator/app/build/outputs/apk/release/app-release-unsigned.apk ./outputs/Calculator.apk
  artifacts:
    paths:
      - ./outputs
    # - CHANGELOG

/* RollingDice *****************************************************************/
lintDebugRollingDice:
  stage: build
  script:
    - ./RollingDice/gradlew -Pci --console=plain :app:lintDebug -PbuildDir=lint

assembleDebugRollingDice:
  stage: build
  script:
    - ./RollingDice/gradlew assembleDebug

testDebugRollingDice:
  stage: test
  script:
    - ./RollingDice/gradlew -Pci --console=plain :app:testDebug

assembleReleaseRollingDice:
  stage: release
  script:
    - echo $KEYSTORE_FILE | base64 -d > my.keystore
    - ./RollingDice/gradlew assembleRelease
      -Pandroid.injected.signing.store.file=$(pwd)/my.keystore
      -Pandroid.injected.signing.store.password=$KEYSTORE_PASSWORD
      -Pandroid.injected.signing.key.alias=$KEY_ALIAS
      # -Pandroid.injected.signing.key.password=$KEY_PASSWORD
    - mv ./RollingDice/app/build/outputs/apk/release/app-release-unsigned.apk ./outputs/RollingDice.apk
  artifacts:
    paths:
      - ./outputs
    # - CHANGELOG



# build:
#   stage: build
#   script:
#     - echo "Building"
#     - mkdir output
#     - cd RollingDice
#     - ./gradlew assemble
#     - cd ../output
#     - mv ../RollingDice/app/build/outputs/apk/debug/app-debug.apk ./RollingDice-debug.apk
#   artifacts:
#     paths:
#       - output/
#
# test:
#   stage: test
#   script:
#     - echo "Testing"
#     - test -f "output/RollingDice-debug.apk"
